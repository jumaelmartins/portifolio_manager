# Legacy SQLite migrations

These migrations predate the PostgreSQL datasource introduced in June 2026.
They are retained for historical reference and are not consumed by Prisma.

The active PostgreSQL migration history lives in `../migrations`.
