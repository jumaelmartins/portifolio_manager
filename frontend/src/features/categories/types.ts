export type CategoryEntry = {
  id: number;
  name: string;
};

export type CategoryInput = {
  name: string;
};

export type BackendCategory = {
  id: number;
  category: string;
};

export type BackendCategoryInput = {
  category: string;
};
